(function() {
  var debug, expect, project, util;

  project = require("../project");

  expect = require('chai').expect;

  util = require("util");

  debug = require("debug")("vsproj:tests:project-test");

  describe('Project', function() {
    return it('Open', function() {
      var proj;
      proj = new project();
      return proj.open("./demo/TestWebApplication/TestWebApplication.csproj").then(function() {
        return expect(true).to.equal(true);
      });
    });
  });

}).call(this);
