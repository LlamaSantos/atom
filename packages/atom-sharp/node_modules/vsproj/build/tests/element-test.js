(function() {
  var Element, expect, util;

  Element = require("../element");

  expect = require('chai').expect;

  util = require("util");

  describe('Element', function() {
    return it('Open', function() {
      var ele;
      ele = new Element;
      ele.push([]);
      return expect(ele.elements.length).to.equal(1);
    });
  });

}).call(this);
