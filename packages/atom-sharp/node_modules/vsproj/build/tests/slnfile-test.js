(function() {
  var debug, expect, slnFile, util;

  slnFile = require("../slnfile");

  expect = require('chai').expect;

  util = require("util");

  debug = require("debug")("vsproj:tests:slnfile-test");

  describe('SlnFile', function() {
    return it('Open Simple', function() {
      var sol;
      sol = new slnFile();
      return sol.open("./demo/solution.sln").then(function() {
        var VisualStudioVersion;
        VisualStudioVersion = sol.getElement("VisualStudioVersion").properties[0];
        return expect(VisualStudioVersion).to.equal("12.0.30501.0");
      });
    });
  });


  /*
    it 'Save Simple', () ->
      sol = new slnFile()
      sol.open("./demo/WebApplication1.sln").then () ->
        sol.save("./demo/slnfile-test.sln").then () ->
          debug "save done"
          expect(true).to.equal(true)
         *console.log "sol: ", util.inspect(sol)
         *debugger
   */

}).call(this);
